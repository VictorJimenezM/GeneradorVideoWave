services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - ./.cert:/app/.cert
    environment:
      - NODE_ENV=development
    command: >
      sh -c "npm run dev"
